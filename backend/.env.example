APP_NAME=NeuroSync AI
DEBUG=false
HOST=0.0.0.0
PORT=8000
ALLOWED_ORIGINS=["http://localhost:3000"]

# Whisper model size: tiny | base | small | medium
WHISPER_MODEL=base
WHISPER_DEVICE=cpu

# Database — project uses SQLite (no external database required)
# DATABASE_URL is reserved for future PostgreSQL migration
# DATABASE_URL=postgresql://user:password@localhost:5432/neurosync
